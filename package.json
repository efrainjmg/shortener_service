{
  "name": "shortener_service",
  "version": "1.0.0",
  "description": "Lambda para acortar URLs - Sistema Distribuido",
  "main": "src/handler.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}